version: "3.9"

services:
  rss-morning:
    build:
      context: .
      args:
        OPENAI_API_KEY: ${OPENAI_API_KEY?Set OPENAI_API_KEY before building}
    image: rss-morning:local
    restart: "no"
